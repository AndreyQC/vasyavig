# План: «Текущая директория» в шапке сайдбара + обновление беклога

Notion-таблицы — в беклог, кода в этой итерации нет.

## 1. Шапка сайдбара (`src/components/Sidebar/SidebarHeader.tsx`)

Новый вид сверху панели:
1. Label «Текущая директория» (gravity `Label`/`Text variant="caption"`, muted) — показывается, когда папка открыта.
2. Под ним — имя директории **bold** (`getFileName(rootPath)` из `src/lib/utils.ts`, `rootPath` уже в `fileStore`), ellipsis, тултип с полным путём.
3. Ниже — кнопка **«Открыть директорию»** (переименование с «Открыть папку») и ряд иконочных действий (без изменений).

- Убрать текущий показ пути в `sidebar-footer` (`src/App.tsx`, строки с `rootPath` + `sidebar-footer`), почистить CSS (`Sidebar.css`) при необходимости.
- i18n (`src/locales/ru/translation.json`, `en/translation.json`):
  - `sidebar.openFolder`: «Открыть директорию» / «Open directory» (или отдельный ключ при нежелании трогать существующий).
  - Новый ключ `sidebar.currentDirectory`: «Текущая директория» / «Current directory».

## 2. Roadmap / беклог (`_docs_/roadmap.md`)

Раздел «Беклог (вне фаз, приоритет уточняется)»:
- Новый пункт **Работа с git**: при открытии папки с репозиторием — status/commit/pull/push из UI (источник: запрос пользователя 2026-08-31).
- Явно зафиксировать запрошенные пункты (частично уже есть): mermaid в WYSIWYG, вставка изображений из буфера и drag-and-drop на окно редактора, изменение размера картинок.
- Переформулировать пункт про Notion-таблицы: целевой формат — YFM `{% table %}` + `{% colwidth=[N] %}`; по исследованию не поддерживается текущими версиями Gravity-редактора и diplodoc (только `#|`-синтаксис, без colwidth) — потребуется собственный парсер/сериализатор и превью-плагин; крупная фаза, наработки `_docs_/GRAMAX_RESEARCH_RESULT.md` §3.

## 3. Проверки и коммиты

- `npx tsc --noEmit`, `npx pnpm@11.24.0 test` — зелёные; ручная проверка в dev-режиме.
- Коммиты раздельно по конвенции: `feat(app): ...` — код, `docs(roadmap): ...` — roadmap.
- Простое изменение — task-файл не нужен (TASK_CONVENTIONS §7).