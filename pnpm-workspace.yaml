# Разрешение build-скриптов (LESSONS_LEARNED.md §1):
# pnpm 10+ по умолчанию блокирует postinstall зависимостей.
# pnpm 12: каноническая форма — allowBuilds (имя -> bool);
# записано командой `pnpm approve-builds esbuild`.
allowBuilds:
  esbuild: true
