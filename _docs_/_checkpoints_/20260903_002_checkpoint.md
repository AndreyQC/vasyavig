# Checkpoint 20260903_002

> Дата: 2026-09-03 | Ветка: dev (= main) | Код-HEAD: a9fb255 Merge PR #5
> dev -> main (2026-09-03)
> Фаза: релизный мердж — весь накопленный разрыв слит в main
> Дальше: следующий change из беклога (git-интеграция / внешние изменения
> файлов / Notion-таблицы / mermaid в WYSIWYG)

## Что это за проект

Vasyavig — desktop-редактор Markdown/YFM на Tauri v2 (Rust) + React 19 + Vite +
TypeScript. Gravity UI, Monaco, zspell, Zustand, i18next. Приложение — в корне.

## Что произошло

- PR #5 `dev` -> `main` смёрджен (2026-09-03): 88 коммитов разрыва — Phase 4–6,
  multi-root workspace (20260901), editor-ux (20260902), просмотр изображений/
  .eml + редактирование текстовых файлов (20260903). Пользовательские
  изменения — в `CHANGELOG.md` (Unreleased).
- Локальный `main` синхронизирован; `dev` и `main` содержат одно состояние.
- Мелочь: `.vite` (кэш vite build) добавлен в `.gitignore` (20f3461).

## Current state (полный свод)

| Зона | Путь | Комментарий |
|---|---|---|
| Спеки | `openspec/specs/` | 9 capabilities: workspace, folder-colors, preview-tabs, layout-stability, wide-content-scrolling, image-viewer, eml-viewer, text-file-editing, workspace-file |
| Архивы change | `openspec/changes/archive/` | 3 change (20260901, 20260902, 20260903) |
| Просмотрщики | `src/components/EditorArea/{ImageViewer,EmlViewer}.tsx` | изображения (зум, SVG безопасно), .eml («Разбор»/«Исходник») |
| MIME-парсер | `src/lib/emlParser.ts` | RFC 5322/2047, QP/base64/8bit, cp1251/koi8-r/utf-8 |
| Rust fs | `src-tauri/src/commands/fs.rs` | + `read_file_latin1` (транспорт не-UTF-8 .eml) |
| Сохранение | `src/store/editorStore.ts` | savable = markdown + text; image/email — просмотр |

## Known gaps / NOT done

- Реакция вкладок на внешние изменения файлов — в беклоге (отдельный change).
- .eml: без HTML-рендера и вложений (решение пользователя 2026-09-03).
- Табы не персистентны между запусками; версии/теги ещё не присваивались.

## Test status

На момент мерджа: vitest 153/153, cargo 13/13, tsc чисто,
`openspec validate --specs` 9/9.

## Where to read more

1. `20260903_001_checkpoint.md` — детали change 20260903.
2. `CHANGELOG.md` — пользовательские изменения Unreleased.
3. `_docs_/roadmap.md` — беклог и текущее состояние.

## Next planned work

1. Выбрать следующий change из беклога (git-интегция — самый частый запрос).
2. При необходимости — присвоить версию (тег) и релизную сборку.
